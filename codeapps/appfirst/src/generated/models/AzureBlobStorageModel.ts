/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 * This file is autogenerated. Do not edit this file directly.
 */

export type Object = object;

export interface BlobMetadata {
  // The unique id of the file or folder.
  Id?: string;
  // The name of the file or folder.
  Name?: string;
  // The display name of the file or folder.
  DisplayName?: string;
  // The path of the file or folder.
  Path?: string;
  // The date and time the file or folder was last modified.
  LastModified?: string;
  // The size of the file or folder.
  Size?: number;
  // The media type of the file or folder.
  MediaType?: string;
  // A boolean value (true, false) to indicate whether or not the blob is a folder.
  IsFolder?: boolean;
  // The etag of the file or folder.
  ETag?: string;
  // The filelocator of the file or folder.
  FileLocator?: string;
}

export interface BlobMetadataResponse {
  // The unique id of the file or folder.
  Id?: string;
  // The name of the file or folder.
  Name?: string;
  // The display name of the file or folder.
  DisplayName?: string;
  // The path of the file or folder.
  Path?: string;
  // The date and time the file or folder was last modified.
  LastModified?: string;
  // The size of the file or folder.
  Size?: number;
  // The media type of the file or folder.
  MediaType?: string;
  // A boolean value (true, false) to indicate whether or not the blob is a folder.
  IsFolder?: boolean;
  // The etag of the file or folder.
  ETag?: string;
  // The filelocator of the file or folder.
  FileLocator?: string;
}

export interface ListOfBlobsWithSensitivityLabels {
  // List of Blobs
  value?: DataWithSensitivityLabelInfo[];
}

export interface DataWithSensitivityLabelInfo {
  // The unique id of the file or folder.
  Id?: string;
  // The name of the file or folder.
  Name?: string;
  // The display name of the file or folder.
  DisplayName?: string;
  // The path of the file or folder.
  Path?: string;
  // The date and time the file or folder was last modified.
  LastModified?: string;
  // The size of the file or folder.
  Size?: number;
  // The media type of the file or folder.
  MediaType?: string;
  // A boolean value (true, false) to indicate whether or not the blob is a folder.
  IsFolder?: boolean;
  // The etag of the file or folder.
  ETag?: string;
  // The filelocator of the file or folder.
  FileLocator?: string;
  // Sensitivity label metadata info list
  SensitivityLabelInfo?: SensitivityLabelMetadata[];
}

export interface BlobMetadataPage {
  // Blob metadata collection.
  value?: BlobMetadata[];
  // An Url which can be used to retrieve the next page.
  nextLink?: string;
  // A marker which can be used to retrieve the next page.
  nextPageMarker?: string;
}

export interface SensitivityLabelMetadata {
  // SensitivityLabel Id.
  sensitivityLabelId?: string;
  // SensitivityLabel name.
  name?: string;
  // SensitivityLabel displayName info
  displayName?: string;
  // SensitivityLabel details on tooltip.
  tooltip?: string;
  // SensitivityLabel priority.
  priority?: number;
  // SensitivityLabel color.
  color?: string;
  //  is  SensitivityLabel Encrypted.
  isEncrypted?: boolean;
  // Whether  SensitivityLabel is Enabled.
  isEnabled?: boolean;
  // Whether  SensitivityLabel is Parent.
  isParent?: boolean;
  // Parent  SensitivityLabel Id.
  parentSensitivityLabelId?: string;
}

export type SharedAccessSignatureBlobPolicyPermissions = 'Read'|'Write'|'Add'|'Create'|'Delete'|'List'|'Read,Write'|'Read,Write,List'|'Read,Write,List,Delete';
export type SharedAccessSignatureBlobPolicyAccessProtocol = 'HttpsOnly'|'HttpsOrHttp';

export interface SharedAccessSignatureBlobPolicy {
  // The string identifying a stored access policy. The Group policy parameters (e.g. Start time and End time) have precedence over input parameters mentioned in actions.
  GroupPolicyIdentifier?: string;
  // The permissions specified on the SAS (Values separated by comma).
  Permissions?: SharedAccessSignatureBlobPolicyPermissions;
  // The date and time at which the SAS becomes valid (example: '2017-11-01T15:30:00+00:00'). Default = now().
  StartTime?: string;
  // The date and time after which the SAS is no longer valid (example: '2017-12-01T15:30:00+00:00'). Default = now() + 24h.
  ExpiryTime?: string;
  // The allowed protocols (https only, or http and https). Null if you don't want to restrict protocol.
  AccessProtocol?: SharedAccessSignatureBlobPolicyAccessProtocol;
  // The allowed IP address or IP address range. Null if you don't want to restrict based on IP address.
  IpAddressOrRange?: string;
}

export interface SharedAccessSignature {
  // A URL to an object with access token.
  WebUrl?: string;
}

export interface StorageAccountList {
  // List of storage account names
  value?: StorageAccount[];
}

export interface StorageAccount {
  // The name of the storage account.
  Name?: string;
  // The display name of the storage account.
  DisplayName?: string;
}

export interface DataSetsMetadata {
  tabular?: TabularDataSetsMetadata;
  blob?: BlobDataSetsMetadata;
}

export interface TabularDataSetsMetadata {
  // Dataset source
  source?: string;
  // Dataset display name
  displayName?: string;
  // Dataset url encoding
  urlEncoding?: string;
  // Table display name
  tableDisplayName?: string;
  // Table plural display name
  tablePluralName?: string;
}

export interface BlobDataSetsMetadata {
  // Blob dataset source
  source?: string;
  // Blob dataset display name
  displayName?: string;
  // Blob dataset url encoding
  urlEncoding?: string;
}
